{
  "name": "@ayurveda/config",
  "version": "0.0.1",
  "private": true,
  "description": "Shared TypeScript and ESLint configuration for the Ayurveda platform",
  "scripts": {
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "rimraf": "^6.0.0"
  }
}
